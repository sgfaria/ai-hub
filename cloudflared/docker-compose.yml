version: "3.9"
services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    restart: unless-stopped
    networks: [ai-network]
networks: {ai-network: {external: true}}
