extends: default

rules:
  document-start: disable
  line-length:
    max: 180
    level: warning
  truthy: disable
  comments:
    min-spaces-from-content: 1
  brackets:
    min-spaces-inside: 0
    max-spaces-inside: 1
