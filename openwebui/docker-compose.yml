version: "3.9"
services:
  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    restart: unless-stopped
    expose: ["8080"]
    volumes: ["./data:/app/backend/data"]
    networks: [ai-network]
networks: {ai-network: {external: true}}
